flask==3.0.3
flask-cors==4.0.1
werkzeug==3.0.3
trimesh==4.4.1
numpy==1.26.4
requests==2.32.3
gunicorn==22.0.0
# Optional: better STEP/IGES support
cascadio==0.0.8

# For STEP/IGES conversion — uncomment ONE of these on Render:
# Option A — cadquery (recommended, easier install)
# cadquery==2.4.0

# Option B — pythonOCC-core (lower level, same engine)
# pythonocc-core==7.7.2
